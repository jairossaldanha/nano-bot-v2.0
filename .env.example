# Cloudflare Zero Trust Tunnel Token
# Get this from: one.dash.cloudflare.com → Networks → Tunnels → your tunnel → Configure
# NEVER commit the real .env to git — only this .env.example
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
